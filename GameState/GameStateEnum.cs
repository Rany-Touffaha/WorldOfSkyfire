namespace WorldOfSkyfire
{
    public enum GameState
    {
        MainMenu,
        Playing,
        GameOver
    }

    public enum Trigger
    {
        StartGame,
        PlayerDied,
        Restart
    }
}